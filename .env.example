TELEGRAM_TOKEN=
DEEPSEEK_API_KEY=
OCR_SPACE_API_KEY=

# Intro media
INTRO_VIDEO_PATH=
INTRO_VIDEO_CAPTION=

# Database
# SQLite локально (файл в корне): sqlite:///medall.db
# Postgres prod: postgresql+psycopg://USER:PASSWORD@HOST:5432/DBNAME
POSTGRES_DB=medall
POSTGRES_USER=medall
POSTGRES_PASSWORD=change_me_strong_password
POSTGRES_HOST=localhost
POSTGRES_PORT=5432

# Local profile (бот запускается с хоста)
# DATABASE_URL=postgresql+psycopg://medall:change_me_strong_password@localhost:5432/medall

# Docker profile (бот запускается в контейнере docker-compose)
# DATABASE_URL=postgresql+psycopg://medall:change_me_strong_password@postgres:5432/medall

# Active DSN for current environment:
DATABASE_URL=

# Analytics
ANALYTICS_LOG_MAX_BYTES=5242880
